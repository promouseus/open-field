# This file controls the activity of snmpd and snmptrapd

# MIB directories.  /usr/share/snmp/mibs is the default, but
# including it here avoids some strange problems.
export MIBDIRS=<%= node['snmp']['snmpd']['mibdirs'] %>
<% unless node['snmp']['snmpd']['mibs'].nil? -%>
export MIBS=<%= node['snmp']['snmpd']['mibs'] %>
<% end -%>

# snmpd control (yes means start daemon).
SNMPDRUN=<%= node['snmp']['snmpd']['snmpd_run'] %>

# snmpd options (use syslog, close stdin/out/err).
SNMPDOPTS='<%= node['snmp']['snmpd']['snmpd_opts'] %>'

# snmptrapd control (yes means start daemon).  As of net-snmp version
# 5.0, master agentx support must be enabled in snmpd before snmptrapd
# can be run.  See snmpd.conf(5) for how to do this.
TRAPDRUN=<%= node['snmp']['snmpd']['trapd_run'] %>

# snmptrapd options (use syslog).
TRAPDOPTS='<%= node['snmp']['snmpd']['trapd_opts'] %>'

# create symlink on Debian legacy location to official RFC path
SNMPDCOMPAT=<%= node['snmp']['snmpd']['snmpd_compat'] %>